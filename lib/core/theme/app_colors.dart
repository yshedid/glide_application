import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xff617AFA);
  static const Color primaryDark = Color(0xff21294A);
  static const Color accent = Color(0xff21294A);
  static const Color bottomNav = Color(0xff171C36);

  // Neutralsg
  static const Color background = Color(0xff0F1224);
  // static const Color surface = Colors.white;
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xff8F99CC);

  // Alerts
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
}
